# Keep Retrofit/serialization generated models
-keepattributes *Annotation*
-keepattributes Signature
-keepattributes InnerClasses

-keepclassmembers class com.institute.user.data.** {
    <fields>;
}
